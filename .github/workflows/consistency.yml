name: consistency

on:
  push:
  workflow_dispatch:
  schedule:
    - cron: "0 4 * * 1"

jobs:
  consistency:
    uses: openads-project/openads-dev-environment/.github/workflows/consistency.yml@main
    secrets: inherit
    with:
      skip-checks: required_root_ci_workflows,readme_generator_is_idempotent
